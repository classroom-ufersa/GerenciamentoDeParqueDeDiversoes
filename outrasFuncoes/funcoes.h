
/*Funcao que direcionara o usuario para determinada opcao selecionada*/
void menuPrincipal(void);

/*Funcao veriricar se a entrada tem apenas 1 caractere*/
int verificarEntradaChar(char entrada[]);

/*Funcao que limpa buffer de entrada (entrada a mais que o necessario)*/
void limpaBuffer(void);